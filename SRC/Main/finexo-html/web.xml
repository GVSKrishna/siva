<build>
    <plugins>
        <plugin>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-maven-plugin</artifactId>
            <version>2.3.8.RELEASE</version>
            <configuration>
                <mainClass>com.example.SpringBootDockerApp</mainClass> <!-- Update with your main class -->
            </configuration>
        </plugin>
    </plugins>
</build>
